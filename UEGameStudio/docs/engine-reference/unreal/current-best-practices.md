# Current Best Practices — Verified Index

本文件只登记经过项目版本核实的 UE 实践，不承载模板默认事实。每条必须包含：`claim`、`applies_to`、`verified_on`、`official_url_or_source_path`、`status`。

当前状态：**EMPTY / FAIL-CLOSED**。完成 `VERSION.md` 初始化前，不得向本文件添加或引用版本敏感结论。

## Entry template

```yaml
- claim: <可验证断言>
  applies_to: UE <major.minor.patch>
  verified_on: YYYY-MM-DD
  official_url_or_source_path: <Epic URL 或目标引擎源码路径>
  status: active
```
