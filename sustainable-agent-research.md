# 可持续自进化 Agent：设计依据

本文件记录 SEA 的问题定义与设计取舍。可执行机制、脚本入口和状态机的权威说明是
`SEA/EVOLUTION.md`；两者冲突时以后者和实际 validator 为准。

## 问题定义

Agent 系统会随着记忆、技能、角色定义与工具适配持续变化。单纯增加内容不能证明
能力提升，反而可能引入冲突、过时事实、供应链风险、上下文膨胀与不可回滚状态。
因此“自进化”必须被建模为受评估器约束的配置演进，而不是生成器自由改写自己。

## 核心假设

1. 可观测证据比自我评价可靠：改动必须绑定测试、失败信号或用户纠正。
2. 评估快照必须不可漂移：生成与裁决之间若正文或用例变化，旧结论失效。
3. 基线必须单调不降：只有严格正增益才进入生效状态。
4. 历史导入不等于验证通过：没有可复核前测的资产只能标为 `captured`。
5. 最小变更更容易归因与回滚：优先记忆，其次技能，再次代码与定义。
6. 长期可持续依赖遗忘：低命中、冲突或过期内容要 deprecated，而非无限累积。
7. 多智能体的主要价值是边界清晰与独立证据，不是无约束地增加角色数量。

## 分层模型

| 层 | 主要对象 | 进入条件 | 退出/回滚条件 |
|---|---|---|---|
| 记忆 | `SEA/memory/*.yaml` | 可验证 claim、evidence、source | 冲突、衰减、失效后 deprecated |
| 技能 | `SKILL.md` + tests | pending、L1 heldout、审计、HITL | 分数不增或审计失败则 reverted |
| 定义 | AGENTS/agent/topology | 改进注册、结构/拓扑评估、HITL | 不高于基线则回滚 |
| 工具 | 脚本/连接器适配 | 重复失败信号与最小修复 | 供应链或回归守卫失败 |
| 群体 | 工作区同步/Hub | 来源与版本可追溯 | 冲突不静默覆盖 |

## 评估可信度

SEA 区分 L0 确定性启发式与 L1 会话模型判官。L0 适合快速发现结构缺失，不能证明
语义满足。L1 v2 请求包含完整技能正文、逐项 assertions、fixture、不可变路径和
SHA-256 快照；回执逐断言给出布尔结果与证据。收集阶段验证正文、用例、请求和判官
模型，避免“评的不是即将生效的版本”。HITL 是独立于模型分数的最终授权门。

## 安全与可回滚性

- 所有持久化产物使用可 diff 文本与 Git 留痕；
- 技能生效前扫描 secret、危险命令、敏感路径与跨技能污染；
- 安装器由 manifest 枚举文件，不递归复制未知内容；
- 版本敏感事实必须有适用版本、官方来源和核实日期；缺失时 fail-closed；
- 自动化不得把 pending 直接改成 solidified，也不得伪造历史 `score_before`。

## 成功标准

成功不是资产数量增长，而是：生命周期覆盖无断点、规范 ID 无悬空、权限最小化、
安装结果可复现、版本事实可核实、测试同时覆盖正向与否定断言、每次改动可归因且可
回滚。健康度与操作入口见 `SEA/EVOLUTION.md` 和 `SEA/scripts/report-metrics.py`。
